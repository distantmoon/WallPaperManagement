@model WallPaperManagement.Models.OrderInfo

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>OrderInfo</legend>

    <div class="display-label">CreateDate</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CreateDate)
    </div>

    <div class="display-label">PriceDecimal</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.PriceDecimal)
    </div>

    <div class="display-label">OrderCount</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.OrderCount)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
